version: "3.8"

services:
  db:
    image: postgres
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
  web:
    build: 
      context: ./conscious_consumer
      dockerfile: ./conscious_consumer/Dockerfile
    command: python ./conscious_consumer/manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    environment:
      - DJANGO_SETTINGS_MODULE=conscious_consumer.caprover_settings
    depends_on:
      - db 
